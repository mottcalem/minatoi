import Link from "next/link";import {ProductArt} from "./ProductArt";import {Product,formatPrice} from "@/app/data";
export function ProductCard({p}:{p:Product}){return <article className="product-card"><Link href={`/urun/${p.slug}`} className="product-image"><ProductArt colors={p.colors} image={p.image}/>{p.badge&&<span className="badge">{p.badge}</span>}<span className="quick">Hızlı incele →</span></Link><div className="product-meta"><div><small>{p.category} · {p.code}</small><Link href={`/urun/${p.slug}`}>{p.name} Cam Tablo</Link></div><div className="price">{p.oldPrice&&<del>{formatPrice(p.oldPrice)}</del>}<b>{formatPrice(p.price)}'den</b></div></div></article>}
